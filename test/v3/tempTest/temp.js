const arr = [
  {
    name: "grammi",
    messages: ["hi", "hello"],
  },
  {
    name: "ryuk",
    messages: ["one", "two", "three"],
  },
];

// let obj = arr.find((o) => o.name === "richard");

// console.log(Boolean(arr.find((o) => o.name === "richard")));
